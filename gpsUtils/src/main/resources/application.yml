spring:
  application:
    name: gpsUtils
server:
  port: 8081

  eureka:
    client:
      registerWithEureka: false
      fetchRegistry: false
      service-url:
        defaultZone: http://localhost:8083/eureka
    instance:
      hostname: localhost
